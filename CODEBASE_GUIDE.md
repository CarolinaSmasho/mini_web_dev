# 🗺️ คู่มือทำความเข้าใจ Codebase - GamerLFG

> **สำหรับ INFJ**: เอกสารนี้จะอธิบายทุกไฟล์และโฟลเดอร์ในโปรเจกต์อย่างละเอียด พร้อมบอกว่า "ทำไม" ถึงออกแบบแบบนี้ และ "ที่ไหน" ที่ควรแก้ไขเมื่อต้องการเพิ่มฟีเจอร์ใหม่

---

## 📋 สารบัญ

1. [ภาพรวมโปรเจกต์](#-ภาพรวมโปรเจกต)
2. [โครงสร้างหลัก](#-โครงสรางหลก)
3. [โฟลเดอร์และไฟล์ทั้งหมด](#-โฟลเดอรและไฟลทงหมด)
4. [คู่มือการแก้ไข: ฉันต้องแก้ตรงไหน?](#-คมอการแกไข-ฉนตองแกตรงไหน)
5. [Data Flow: ข้อมูลไหลยังไง?](#-data-flow-ขอมลไหลยงไง)

---

## 🎯 ภาพรวมโปรเจกต์

### GamerLFG คืออะไร?
แพลตฟอร์มสำหรับเกมเมอร์ในการหาเพื่อนร่วมเล่นเกม (Looking For Group) ที่ออกแบบมาเพื่อแก้ปัญหาการหาปาร์ตี้ที่ "ใช่" สำหรับเกมเมอร์

### เทคโนโลยีที่ใช้
- **Backend**: ASP.NET Core MVC (.NET 10)
- **Database**: MongoDB (NoSQL)
- **Frontend**: Razor Views + Vanilla CSS/JavaScript
- **Architecture Pattern**: Repository Pattern + MVC

### ปรัชญาการออกแบบ
โปรเจกต์นี้ใช้ **Separation of Concerns** แยกหน้าที่ของแต่ละส่วนอย่างชัดเจน:
- **Models** = โครงสร้างข้อมูล (Data Structure)
- **Views** = ส่วนที่ผู้ใช้เห็น (UI)
- **Controllers** = ตัวกลางระหว่าง Views และ Data (Logic)
- **Repositories** = ติดต่อกับ Database (Data Access)
- **Services** = Business Logic ที่ซับซ้อน (Complex Operations)

---

## 🏗️ โครงสร้างหลัก

```
mini_webdev/
├── GamerLFG/              # โปรเจกต์หลัก (ทุกอย่างอยู่ที่นี่)
├── README.md              # คู่มือการติดตั้งและรันโปรเจกต์
├── api-contract.md        # สัญญา API ระหว่าง Frontend-Backend
├── features.md            # รายละเอียดฟีเจอร์ทั้งหมด
├── diagram.md             # Diagrams (Class, ERD, Sequence)
└── CODEBASE_GUIDE.md      # ไฟล์นี้ที่คุณกำลังอ่าน
```

---

## 📁 โฟลเดอร์และไฟล์ทั้งหมด

### 📂 `/GamerLFG/` - โปรเจกต์หลัก

#### 🎮 **ทำไมถึงมีโฟลเดอร์นี้?**
นี่คือหัวใจของโปรเจกต์ทั้งหมด ทุกโค้ดที่ทำให้แอปทำงานได้อยู่ในนี้

---

### 📂 `/GamerLFG/Controllers/` - ตัวควบคุมทั้งหมด

**Controllers คือตัวกลางที่รับ Request จากผู้ใช้ แล้วส่งต่อไปยัง Models/Services และส่ง Response กลับไป**

#### 📄 `HomeController.cs`
- **ทำอะไร**: จัดการหน้าแรก (Activity Feed)
- **ใช้เมื่อไหร่**: เมื่อผู้ใช้เข้าหน้าแรกของเว็บ
- **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยนหน้าแรก หรือเพิ่มส่วนใหม่ในหน้า Feed

#### 📄 `LobbyController.cs` ⭐ สำคัญมาก
- **ทำอะไร**: จัดการทุกอย่างเกี่ยวกับ Lobby (สร้าง, แก้ไข, ดูรายละเอียด)
- **ใช้เมื่อไหร่**: เมื่อผู้ใช้สร้าง Lobby, ดู Lobby, แก้ไข Lobby
- **แก้ตรงนี้เมื่อ**: 
  - ต้องการเพิ่มฟีเจอร์ใหม่ใน Lobby (เช่น เพิ่มฟิลด์ใหม่)
  - ต้องการเปลี่ยนวิธีการแสดงผล Lobby
- **ตัวอย่าง Methods**:
  - `Index()` - แสดงรายการ Lobby ทั้งหมด
  - `Create()` - สร้าง Lobby ใหม่
  - `Edit(id)` - แก้ไข Lobby
  - `Details(id)` - ดูรายละเอียด Lobby

#### 📄 `UserController.cs`
- **ทำอะไร**: จัดการโปรไฟล์ผู้ใช้, การตั้งค่า, รายชื่อเพื่อน
- **ใช้เมื่อไหร่**: เมื่อผู้ใช้ดูโปรไฟล์, แก้ไขข้อมูล, ดูรายชื่อเพื่อน
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มฟีเจอร์เกี่ยวกับผู้ใช้ (เช่น เพิ่มฟิลด์ใหม่ในโปรไฟล์)

#### 📄 `AuthController.cs`
- **ทำอะไร**: จัดการการ Login, Register, Logout
- **ใช้เมื่อไหร่**: เมื่อผู้ใช้ต้องการเข้าสู่ระบบหรือสมัครสมาชิก
- **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยนวิธีการ Authentication

#### 📄 `LobbyApiController.cs` ⚡ API
- **ทำอะไร**: API สำหรับ AJAX calls (ไม่ return HTML แต่ return JSON)
- **ใช้เมื่อไหร่**: เมื่อ JavaScript ต้องการดึงข้อมูลหรือส่งข้อมูลแบบ Asynchronous
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่ม API endpoint ใหม่สำหรับ Lobby
- **ตัวอย่าง**: `/api/lobbies`, `/api/lobbies/{id}/apply`

#### 📄 `UserApiController.cs` ⚡ API
- **ทำอะไร**: API สำหรับจัดการข้อมูลผู้ใช้แบบ JSON
- **ใช้เมื่อไหร่**: เมื่อต้องการดึงข้อมูลผู้ใช้ผ่าน AJAX

#### 📄 `AuthApiController.cs` ⚡ API
- **ทำอะไร**: API สำหรับ Login/Register แบบ JSON
- **ใช้เมื่อไหร่**: ถ้าต้องการทำ Login แบบ AJAX (ไม่ reload หน้า)

#### 📄 `KarmaApiController.cs` ⚡ API
- **ทำอะไร**: API สำหรับระบบ Karma (คะแนนความประพฤติ)
- **ใช้เมื่อไหร่**: เมื่อต้องการให้คะแนนผู้เล่น

#### 📄 `NotificationApiController.cs` ⚡ API
- **ทำอะไร**: API สำหรับการแจ้งเตือน
- **ใช้เมื่อไหร่**: เมื่อต้องการดึงการแจ้งเตือนของผู้ใช้

> **💡 เคล็ดลับ**: Controllers ที่มี "Api" ในชื่อจะ return JSON (สำหรับ AJAX) ส่วนที่ไม่มีจะ return HTML (Razor Views)

---

### 📂 `/GamerLFG/Models/` - โครงสร้างข้อมูล

**Models คือ "แบบจำลอง" ของข้อมูลในระบบ กำหนดว่าข้อมูลแต่ละชนิดมีอะไรบ้าง**

#### 📄 `User.cs` 👤
```csharp
// ตัวอย่างโครงสร้าง
public class User {
    public string Id { get; set; }           // รหัสผู้ใช้
    public string Username { get; set; }     // ชื่อผู้ใช้
    public string Email { get; set; }        // อีเมล
    public double KarmaScore { get; set; }   // คะแนนความประพฤติ
    public List<string> VibeTags { get; set; }    // Tag บุคลิก
    public List<string> GameLibrary { get; set; } // เกมที่มี
}
```
- **ทำอะไร**: กำหนดโครงสร้างข้อมูลผู้ใช้
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มข้อมูลใหม่ของผู้ใช้ (เช่น เพิ่ม Bio, Discord ID)

#### 📄 `Lobby.cs` 🎮
```csharp
public class Lobby {
    public string Id { get; set; }
    public string Title { get; set; }        // ชื่อ Lobby
    public string Game { get; set; }         // เกมที่เล่น
    public string HostId { get; set; }       // รหัสผู้สร้าง
    public int MaxPlayers { get; set; }      // จำนวนคนสูงสุด
    public List<string> Moods { get; set; }  // Tag บรรยากาศ
    public List<Role> Roles { get; set; }    // ตำแหน่งที่ต้องการ
    public bool IsRecruiting { get; set; }   // เปิดรับสมัครหรือไม่
}
```
- **ทำอะไร**: กำหนดโครงสร้างข้อมูล Lobby
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มฟิลด์ใหม่ใน Lobby (เช่น เพิ่ม Voice Chat Link)

#### 📄 `Application.cs` 📝
- **ทำอะไร**: คำขอเข้าร่วม Lobby
- **ประกอบด้วย**: ผู้สมัคร, Lobby ที่สมัคร, Role ที่ต้องการ, ข้อความ
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มข้อมูลในการสมัคร (เช่น เพิ่ม Availability)

#### 📄 `Member.cs` 👥
- **ทำอะไร**: สมาชิกใน Lobby
- **ประกอบด้วย**: ผู้ใช้, Lobby, Role ที่ได้รับ, เวลาเข้าร่วม

#### 📄 `Role.cs` 🎭
- **ทำอะไร**: ตำแหน่ง/บทบาทใน Lobby
- **ประกอบด้วย**: ชื่อ Role, จำนวนที่ต้องการ

#### 📄 `Vote.cs` 🗳️
- **ทำอะไร**: การโหวตรับ/ไม่รับผู้สมัคร
- **ประกอบด้วย**: ผู้โหวต, ผู้สมัคร, ประเภทโหวต (Yes/No)

#### 📄 `Endorsement.cs` ⭐
- **ทำอะไร**: การให้คะแนน/รีวิวผู้เล่น
- **ประกอบด้วย**: ผู้ให้, ผู้รับ, ประเภท (Strategist, Friendly, Skilled)

#### 📄 `KarmaHistory.cs` 📊
- **ทำอะไร**: ประวัติการเปลี่ยนแปลงคะแนน Karma
- **ประกอบด้วย**: ผู้ใช้, คะแนนที่เปลี่ยน, เหตุผล

#### 📄 `Notification.cs` 🔔
- **ทำอะไร**: การแจ้งเตือน
- **ประกอบด้วย**: ผู้รับ, ข้อความ, ประเภท, อ่านแล้วหรือยัง

#### 📄 `FriendRequest.cs` 🤝
- **ทำอะไร**: คำขอเป็นเพื่อน
- **ประกอบด้วย**: ผู้ส่ง, ผู้รับ, สถานะ

#### 📄 `ErrorViewModel.cs` ❌
- **ทำอะไร**: แสดงข้อผิดพลาด
- **ใช้เมื่อไหร่**: เมื่อเกิด Error ในระบบ

> **💡 เคล็ดลับ**: เมื่อต้องการเพิ่มข้อมูลใหม่ ให้เริ่มที่ Models เสมอ เพราะนี่คือ "แบบพิมพ์" ของข้อมูล

---

### 📂 `/GamerLFG/Views/` - หน้าตาที่ผู้ใช้เห็น

**Views คือ HTML ที่ใช้ Razor Syntax (C# + HTML) เพื่อแสดงข้อมูล**

#### 📂 `Views/Home/`
- **`Index.cshtml`** - หน้าแรก (Activity Feed)
  - แสดง My Lobbies, Pending Requests, Public Feed
  - **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยนหน้าตาหน้าแรก

#### 📂 `Views/Lobby/`
- **`Index.cshtml`** - รายการ Lobby ทั้งหมด
- **`Create.cshtml`** - ฟอร์มสร้าง Lobby ใหม่
  - **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มฟิลด์ในฟอร์มสร้าง Lobby
- **`Edit.cshtml`** - หน้าจัดการ Lobby (สำหรับ Host)
  - แสดงสมาชิก, ผู้สมัคร, แชท
  - **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มฟีเจอร์ในหน้าจัดการ
- **`Details.cshtml`** - ดูรายละเอียด Lobby (สำหรับผู้สมัคร)
  - **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยนหน้าตารายละเอียด Lobby

#### 📂 `Views/User/`
- **`Profile.cshtml`** - โปรไฟล์ผู้ใช้
  - **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มข้อมูลในโปรไฟล์
- **`Settings.cshtml`** - การตั้งค่า
  - **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มตัวเลือกการตั้งค่า
- **`Friends.cshtml`** - รายชื่อเพื่อน
- **`EditProfile.cshtml`** - แก้ไขโปรไฟล์
- **`EditGameLibrary.cshtml`** - แก้ไขรายการเกม

#### 📂 `Views/Auth/`
- **`Login.cshtml`** - หน้า Login
- **`Register.cshtml`** - หน้าสมัครสมาชิก

#### 📂 `Views/Shared/`
- **`_Layout.cshtml`** ⭐ สำคัญมาก
  - **ทำอะไร**: Template หลักของทุกหน้า (Header, Footer, Navigation)
  - **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยน Navigation Bar, Footer, หรือโครงสร้างหลักของเว็บ
- **`_ValidationScriptsPartial.cshtml`** - Scripts สำหรับ Validation
- **`Error.cshtml`** - หน้าแสดง Error
- **`_Navbar.cshtml`** - Navigation Bar
- **`_Footer.cshtml`** - Footer

#### 📄 `_ViewImports.cshtml`
- **ทำอะไร**: Import namespaces ที่ใช้ในทุก View
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่ม using statement ที่ใช้ในทุกหน้า

#### 📄 `_ViewStart.cshtml`
- **ทำอะไร**: กำหนด Layout เริ่มต้นสำหรับทุก View
- **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยน Layout เริ่มต้น

> **💡 เคล็ดลับ**: ไฟล์ที่ขึ้นต้นด้วย `_` คือ Partial Views หรือ Shared Components

---

### 📂 `/GamerLFG/Repositories/` - ติดต่อกับ Database

**Repositories คือชั้นที่ติดต่อกับ MongoDB โดยตรง แยกออกจาก Controllers เพื่อให้เปลี่ยน Database ได้ง่าย**

#### 🔌 Interface Files (I*.cs)
- **`IUserRepository.cs`** - สัญญาว่า UserRepository ต้องมี methods อะไรบ้าง
- **`ILobbyRepository.cs`** - สัญญาสำหรับ LobbyRepository
- **`IApplicationRepository.cs`** - สัญญาสำหรับ ApplicationRepository
- และอื่นๆ

**ทำไมต้องมี Interface?**
- เพื่อให้เปลี่ยน Implementation ได้ง่าย (เช่น เปลี่ยนจาก MongoDB เป็น SQL Server)
- ทำให้ Test ง่ายขึ้น (สามารถ Mock ได้)

#### 🔧 Implementation Files (Mongo*.cs)
- **`MongoUserRepository.cs`** - ติดต่อกับ MongoDB Users Collection
  - Methods: `GetByIdAsync()`, `CreateAsync()`, `UpdateAsync()`, `DeleteAsync()`
- **`MongoLobbyRepository.cs`** - ติดต่อกับ MongoDB Lobbies Collection
- **`MongoApplicationRepository.cs`** - ติดต่อกับ MongoDB Applications Collection
- และอื่นๆ

**แก้ตรงนี้เมื่อ**:
- ต้องการเพิ่ม Query ใหม่ (เช่น ค้นหา Lobby ตามเกม)
- ต้องการเปลี่ยนวิธีการดึงข้อมูล

> **💡 เคล็ดลับ**: ถ้าต้องการเพิ่ม Entity ใหม่ ต้องสร้างทั้ง Interface และ Implementation

---

### 📂 `/GamerLFG/Services/` - Business Logic ที่ซับซ้อน

**Services คือที่รวม Business Logic ที่ซับซ้อนเกินกว่า Controllers จะจัดการเอง**

#### 📄 `KarmaService.cs` ⭐
- **ทำอะไร**: คำนวณและจัดการคะแนน Karma
- **ใช้เมื่อไหร่**: เมื่อมีการให้ Endorsement, ถูก Recruit, ถูก Reject
- **Logic ที่มี**:
  - คำนวณคะแนนจาก Endorsement แต่ละประเภท
  - บันทึกประวัติการเปลี่ยนแปลง Karma
  - อัพเดท KarmaScore ของผู้ใช้
- **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยนวิธีคำนวณ Karma

#### 📄 `NotificationService.cs` 🔔
- **ทำอะไร**: สร้างและส่งการแจ้งเตือน
- **ใช้เมื่อไหร่**: เมื่อมีเหตุการณ์สำคัญ (ถูก Recruit, ถูก Reject, มีคนสมัคร)
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มประเภทการแจ้งเตือนใหม่

#### 📄 `MongoDbSeeder.cs` 🌱
- **ทำอะไร**: สร้างข้อมูลตัวอย่างใน Database
- **ใช้เมื่อไหร่**: เมื่อรันโปรเจกต์ครั้งแรก (ถ้า Database ว่าง)
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่มข้อมูลตัวอย่างใหม่

> **💡 เคล็ดลับ**: ถ้า Logic ซับซ้อนและใช้ในหลาย Controllers ให้ย้ายมาที่ Services

---

### 📂 `/GamerLFG/Data/` - การเชื่อมต่อ Database

#### 📄 `MongoDbContext.cs` 🔌
- **ทำอะไร**: เชื่อมต่อกับ MongoDB และเข้าถึง Collections
- **ประกอบด้วย**:
  - `Users` Collection
  - `Lobbies` Collection
  - `Applications` Collection
  - และอื่นๆ
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่ม Collection ใหม่

#### 📄 `MongoDbSettings.cs` ⚙️
- **ทำอะไร**: กำหนดโครงสร้างการตั้งค่า MongoDB
- **ประกอบด้วย**: ConnectionString, DatabaseName

---

### 📂 `/GamerLFG/wwwroot/` - Static Files

**wwwroot คือที่เก็บไฟล์ที่ไม่ต้อง Compile (CSS, JavaScript, รูปภาพ)**

#### 📂 `wwwroot/css/`
- **`site.css`** - CSS หลักของเว็บ
  - **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยนสี, ฟอนต์, Layout
- **`lobby.css`** - CSS เฉพาะสำหรับหน้า Lobby
  - **แก้ตรงนี้เมื่อ**: ต้องการปรับแต่งหน้า Lobby

#### 📂 `wwwroot/js/`
- **`site.js`** - JavaScript หลักของเว็บ
  - **แก้ตรงนี้เมื่อ**: ต้องการเพิ่ม Interactive Features

#### 📂 `wwwroot/lib/`
- **ทำอะไร**: เก็บ Libraries ภายนอก (Bootstrap, jQuery, etc.)
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่ม Library ใหม่

#### 📄 `wwwroot/favicon.ico`
- **ทำอะไร**: ไอคอนที่แสดงบน Browser Tab

---

### 📂 `/GamerLFG/Properties/`
- **`launchSettings.json`** - การตั้งค่าการรันโปรเจกต์ (Port, Environment)

---

### 📄 ไฟล์สำคัญในรากโปรเจกต์

#### 📄 `Program.cs` ⭐ สำคัญที่สุด
- **ทำอะไร**: จุดเริ่มต้นของโปรเจกต์ทั้งหมด
- **ประกอบด้วย**:
  - การตั้งค่า Services (Dependency Injection)
  - การตั้งค่า MongoDB
  - การตั้งค่า Session
  - การตั้งค่า Swagger (API Documentation)
  - การ Seed Database
- **แก้ตรงนี้เมื่อ**:
  - ต้องการเพิ่ม Service ใหม่
  - ต้องการเปลี่ยนการตั้งค่า Middleware
  - ต้องการเปลี่ยน Default Route

#### 📄 `appsettings.json` ⚙️
- **ทำอะไร**: การตั้งค่าหลักของโปรเจกต์
- **ประกอบด้วย**:
  - MongoDB ConnectionString
  - Database Name
- **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยน Database หรือเพิ่มการตั้งค่าใหม่

#### 📄 `appsettings.Development.json`
- **ทำอะไร**: การตั้งค่าสำหรับ Development Environment
- **แก้ตรงนี้เมื่อ**: ต้องการตั้งค่าที่ต่างจาก Production

#### 📄 `GamerLFG.csproj` 📦
- **ทำอะไร**: ไฟล์โปรเจกต์ของ .NET (กำหนด Dependencies, Target Framework)
- **แก้ตรงนี้เมื่อ**: ต้องการเพิ่ม NuGet Package ใหม่

#### 📄 `DESIGN.md` 🎨
- **ทำอะไร**: Design System (สี, ฟอนต์, Component Styles)
- **ใช้เมื่อไหร่**: เมื่อต้องการทราบว่าควรใช้สีอะไร, ฟอนต์อะไร
- **แก้ตรงนี้เมื่อ**: ต้องการเปลี่ยน Design System

#### 📄 `SITE.md`
- **ทำอะไร**: บันทึกเกี่ยวกับเว็บไซต์

---

### 📄 ไฟล์เอกสารในรากโปรเจกต์

#### 📄 `README.md` 📖
- **ทำอะไร**: คู่มือการติดตั้งและรันโปรเจกต์
- **ใช้เมื่อไหร่**: เมื่อต้องการรู้วิธีรันโปรเจกต์

#### 📄 `features.md` ✨
- **ทำอะไร**: รายละเอียดฟีเจอร์ทั้งหมดของโปรเจกต์
- **ใช้เมื่อไหร่**: เมื่อต้องการทราบว่าโปรเจกต์มีฟีเจอร์อะไรบ้าง

#### 📄 `api-contract.md` 📋
- **ทำอะไร**: สัญญา API ระหว่าง Frontend และ Backend
- **ใช้เมื่อไหร่**: เมื่อต้องการรู้ว่า API แต่ละตัว Request/Response เป็นอย่างไร
- **แก้ตรงนี้เมื่อ**: เพิ่ม API endpoint ใหม่

#### 📄 `diagram.md` 📊
- **ทำอะไร**: Diagrams ต่างๆ (Class Diagram, ERD, Sequence Diagram)
- **ใช้เมื่อไหร่**: เมื่อต้องการเข้าใจโครงสร้างระบบโดยรวม

---

## 🔧 คู่มือการแก้ไข: ฉันต้องแก้ตรงไหน?

### 🎯 สถานการณ์ที่ 1: ต้องการเพิ่มฟิลด์ใหม่ใน Lobby (เช่น Discord Link)

**ขั้นตอน**:
1. **เพิ่มใน Model** → `Models/Lobby.cs`
   ```csharp
   public string DiscordLink { get; set; }
   ```

2. **เพิ่มใน View (Create)** → `Views/Lobby/Create.cshtml`
   ```html
   <input type="text" name="DiscordLink" placeholder="Discord Link" />
   ```

3. **เพิ่มใน View (Edit)** → `Views/Lobby/Edit.cshtml`
   ```html
   <input type="text" name="DiscordLink" value="@Model.DiscordLink" />
   ```

4. **เพิ่มใน View (Details)** → `Views/Lobby/Details.cshtml`
   ```html
   <p>Discord: @Model.DiscordLink</p>
   ```

5. **อัพเดท Controller** → `Controllers/LobbyController.cs`
   - ไม่ต้องแก้อะไร (ASP.NET Model Binding จะจัดการให้)

6. **อัพเดท API Contract** → `api-contract.md`
   - เพิ่ม `discordLink` ใน Request/Response

---

### 🎯 สถานการณ์ที่ 2: ต้องการเพิ่มฟีเจอร์ใหม่ (เช่น ระบบ Chat)

**ขั้นตอน**:
1. **สร้าง Model** → `Models/Message.cs`
   ```csharp
   public class Message {
       public string Id { get; set; }
       public string LobbyId { get; set; }
       public string SenderId { get; set; }
       public string Content { get; set; }
       public DateTime SentAt { get; set; }
   }
   ```

2. **สร้าง Repository Interface** → `Repositories/IMessageRepository.cs`
   ```csharp
   public interface IMessageRepository {
       Task<List<Message>> GetByLobbyIdAsync(string lobbyId);
       Task CreateAsync(Message message);
   }
   ```

3. **สร้าง Repository Implementation** → `Repositories/MongoMessageRepository.cs`

4. **เพิ่ม Collection ใน MongoDbContext** → `Data/MongoDbContext.cs`
   ```csharp
   public IMongoCollection<Message> Messages => _database.GetCollection<Message>("Messages");
   ```

5. **Register Repository ใน Program.cs**
   ```csharp
   builder.Services.AddScoped<IMessageRepository, MongoMessageRepository>();
   ```

6. **สร้าง API Controller** → `Controllers/MessageApiController.cs`

7. **สร้าง View** → `Views/Lobby/Chat.cshtml`

8. **เพิ่ม JavaScript สำหรับ AJAX** → `wwwroot/js/chat.js`

---

### 🎯 สถานการณ์ที่ 3: ต้องการเปลี่ยนสีของเว็บ

**ขั้นตอน**:
1. **อัพเดท Design System** → `GamerLFG/DESIGN.md`
   - เปลี่ยนค่าสีใน Color Palette

2. **อัพเดท CSS** → `wwwroot/css/site.css`
   ```css
   :root {
       --primary-orange: #FF6600; /* เปลี่ยนจาก #FF9900 */
   }
   ```

---

### 🎯 สถานการณ์ที่ 4: ต้องการเปลี่ยน Navigation Bar

**ขั้นตอน**:
1. **แก้ไข** → `Views/Shared/_Navbar.cshtml`
   - เพิ่ม/ลบ Menu Items
   - เปลี่ยน Logo
   - เปลี่ยน Layout

---

### 🎯 สถานการณ์ที่ 5: ต้องการเพิ่ม API Endpoint ใหม่

**ขั้นตอน**:
1. **เพิ่ม Method ใน API Controller** → `Controllers/LobbyApiController.cs`
   ```csharp
   [HttpGet("api/lobbies/featured")]
   public async Task<IActionResult> GetFeaturedLobbies() {
       // Logic here
   }
   ```

2. **อัพเดท API Contract** → `api-contract.md`
   - เพิ่ม Endpoint, Request, Response

3. **เพิ่ม JavaScript สำหรับเรียก API** → `wwwroot/js/site.js`
   ```javascript
   async function getFeaturedLobbies() {
       const response = await fetch('/api/lobbies/featured');
       const data = await response.json();
       // Handle data
   }
   ```

---

### 🎯 สถานการณ์ที่ 6: ต้องการเปลี่ยนวิธีคำนวณ Karma

**ขั้นตอน**:
1. **แก้ไข** → `Services/KarmaService.cs`
   - เปลี่ยน Logic ในการคำนวณคะแนน
   - เปลี่ยนน้ำหนักของแต่ละ Endorsement Type

---

## 🌊 Data Flow: ข้อมูลไหลยังไง?

### ตัวอย่าง: ผู้ใช้สร้าง Lobby ใหม่

```
1. ผู้ใช้กรอกฟอร์ม → Views/Lobby/Create.cshtml
                      ↓
2. กด Submit → POST /Lobby/Create
                      ↓
3. Controller รับข้อมูล → Controllers/LobbyController.cs
                      ↓
4. Controller เรียก Repository → Repositories/MongoLobbyRepository.cs
                      ↓
5. Repository บันทึกลง MongoDB → Data/MongoDbContext.cs
                      ↓
6. ส่ง Response กลับ → Redirect to /Lobby/Details/{id}
                      ↓
7. แสดงหน้ารายละเอียด → Views/Lobby/Details.cshtml
```

### ตัวอย่าง: AJAX Call ดึงรายการ Lobby

```
1. JavaScript เรียก API → fetch('/api/lobbies')
                      ↓
2. API Controller รับ Request → Controllers/LobbyApiController.cs
                      ↓
3. Controller เรียก Repository → Repositories/MongoLobbyRepository.cs
                      ↓
4. Repository ดึงข้อมูลจาก MongoDB → Data/MongoDbContext.cs
                      ↓
5. ส่ง JSON กลับ → { "success": true, "data": [...] }
                      ↓
6. JavaScript รับข้อมูล → แสดงผลบนหน้าเว็บ
```

---

## 🎓 หลักการสำคัญที่ควรจำ

### 1. **Separation of Concerns** (แยกหน้าที่ชัดเจน)
- **Models** = โครงสร้างข้อมูล
- **Views** = หน้าตา
- **Controllers** = Logic
- **Repositories** = Database Access
- **Services** = Business Logic ที่ซับซ้อน

### 2. **Repository Pattern** (แยก Data Access)
- ทำให้เปลี่ยน Database ได้ง่าย
- ทำให้ Test ง่ายขึ้น
- แยก Logic ออกจาก Controllers

### 3. **Dependency Injection** (ฉีดความสามารถเข้าไป)
- Register ใน `Program.cs`
- ใช้ใน Controllers ผ่าน Constructor
- ทำให้ Code ยืดหยุ่นและ Test ได้ง่าย

### 4. **MVC Pattern** (Model-View-Controller)
- **Model** = ข้อมูล
- **View** = การแสดงผล
- **Controller** = ตัวกลาง

---

## 🚀 เริ่มต้นแก้ไขโปรเจกต์

### ขั้นตอนแนะนำสำหรับมือใหม่

1. **อ่าน README.md** เพื่อรู้วิธีรันโปรเจกต์
2. **อ่าน features.md** เพื่อรู้ว่าโปรเจกต์มีฟีเจอร์อะไรบ้าง
3. **ดู diagram.md** เพื่อเข้าใจโครงสร้างระบบ
4. **อ่าน api-contract.md** เพื่อรู้ว่า API ทำงานอย่างไร
5. **ลองรันโปรเจกต์** และเล่นดูฟีเจอร์ต่างๆ
6. **เริ่มแก้ไขจากสิ่งเล็กๆ** (เช่น เปลี่ยนสี, เปลี่ยนข้อความ)
7. **ค่อยๆ เพิ่มฟีเจอร์ใหม่** ตามขั้นตอนในคู่มือนี้

---

## 💡 Tips สำหรับ INFJ

### เข้าใจ "ทำไม" ก่อน "ทำอย่างไร"
- โปรเจกต์นี้ใช้ **Repository Pattern** เพราะต้องการแยก Data Access ออกจาก Business Logic
- ใช้ **MVC** เพราะต้องการแยกหน้าที่ชัดเจน ทำให้แก้ไขง่าย
- ใช้ **MongoDB** เพราะข้อมูลมีโครงสร้างที่ยืดหยุ่น (Flexible Schema)

### เริ่มจากภาพรวม แล้วค่อยลงรายละเอียด
1. เข้าใจว่าโปรเจกต์ทำอะไร (GamerLFG = หาเพื่อนเล่นเกม)
2. เข้าใจโครงสร้างหลัก (MVC + Repository)
3. เข้าใจ Data Flow (ข้อมูลไหลยังไง)
4. ค่อยลงรายละเอียดแต่ละไฟล์

### ใช้ Diagrams ช่วยเข้าใจ
- ดู Class Diagram เพื่อเข้าใจความสัมพันธ์ระหว่าง Models
- ดู Sequence Diagram เพื่อเข้าใจ Flow ของระบบ
- ดู ERD เพื่อเข้าใจโครงสร้าง Database

---

## 📚 เอกสารอ้างอิง

- [README.md](README.md) - คู่มือการติดตั้งและรัน
- [features.md](features.md) - รายละเอียดฟีเจอร์
- [api-contract.md](api-contract.md) - API Documentation
- [diagram.md](diagram.md) - System Diagrams
- [DESIGN.md](GamerLFG/DESIGN.md) - Design System

---

**สร้างเมื่อ**: 2026-02-16  
**เวอร์ชัน**: 1.0  
**สร้างโดย**: Antigravity AI

*หวังว่าคู่มือนี้จะช่วยให้คุณเข้าใจ Codebase ได้ดีขึ้น และสามารถแก้ไขโปรเจกต์ได้อย่างมั่นใจ! 🚀*
